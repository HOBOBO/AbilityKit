# Mock Integration

该示例用于验证 `Ability Explain` 窗口的 UI 骨架是否工作正常。

导入本 Sample 后，将自动注册：

- `IEntityProvider`
- `IExplainResolver`
- `INavigator`

然后在 Unity 菜单打开：`Tools/Ability Explain` 即可看到示例 Forest/Tree。
